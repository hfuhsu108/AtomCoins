{
  "name": "atomcoins-functions",
  "description": "AtomCoins Web Push 推播 — Cloud Functions（VAPID web-push，非 FCM）",
  "type": "module",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "copy:shared": "node copy-shared.mjs",
    "deploy": "firebase deploy --only functions,firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "web-push": "^3.6.7"
  }
}
