# 更新日誌

本專案遵循 [語意化版本](https://semver.org/lang/zh-TW/)；發布流程＝bump `package.json` 版號 → 補本檔 → push（自動部署）→ 打 git tag。

## v1.7.0 — 2026-08-05

> 設定頁重新分組、三個清單改成左滑操作、借貸期初餘額可隨時調整、發票歸帳會依過往紀錄自動選帳戶。**已測試驗收通過**。

### 新增
- **借貸對象的期初餘額可以隨時調整**：設定 → 借貸對象 → 點該對象即可改金額、方向、起算日
  - 以前只有「新增對象」時能填一次，事後就改不了；常常是對方還錢時才想起當初到底借了多少
  - 金額清空後儲存＝刪除那筆期初（會先問一次）
  - 兩個防呆：金額不能低於已經登錄的還款總額；已經有還款記錄時方向鎖住（改方向會讓還款的錢流向整個反過來）
- **發票歸帳會自動選帳戶**：依這家店過去都用哪個帳戶付來預選
  - 帳戶下方會寫明理由，例如「依『全家』過去 5 筆紀錄自動選填帳戶」
  - 自己動手改過就不再自動填，也不會被蓋掉；沒有歷史紀錄的新店家維持原本的預設帳戶
  - 與發票的分類建議用同一套商家比對，但這項是當場算的，剛記的帳馬上就會納入判斷

### 調整
- **設定頁重新分組**：11 個項目改成五群 —— 主檔設定、輸入輔助、偏好設定、資料同步，關於與更新仍在最底下
  - 「外觀主題」從主頁底部搬進「偏好設定」，成為正式的選單項目
- **標籤、券商、商家別名三個清單支援左滑**：單擊列＝編輯，左滑出現「刪除」
  - 商家別名列上的垃圾桶鈕、標籤列上的鉛筆鈕都收進抽屜了，版面更乾淨
  - 帳戶與分類維持原樣（列上有上下排序箭頭，抽屜動作點完會自動收起，不適合連續調順序）

### 修正
- **從借貸明細編輯期初借貸，會把帳戶從「無」悄悄改成預設帳戶**：期初的本金不屬於任何帳戶，這是刻意的設計；金額不會算錯，但存一次就破壞掉

## v1.6.0 — 2026-08-05

> 週期性收支可以事先設定、範本可在設定頁建立與修改、圖示與顏色大幅增加、借貸對象可填期初餘額。**已測試驗收通過**。

### 新增
- **週期性收支可以事先設定**：設定 → 週期性收支 → 新增，不必先真的記一筆
  - 開的是完整的記帳表單，拆帳、標籤、商家、備註、信用卡帳戶都照樣可填
  - 可指定**每月幾號**或**每週星期幾**，選了之後「首次發生日」會自動跳到最近一個符合的日子，也可以自己改
  - 可設定**間隔**：每 2 個月、每 3 週都行（以前固定只能每期一次）
  - 既有規則現在也**可以編輯內容**了（以前只能暫停或刪除）
  - 首次發生日設成今天＋自動入帳的話，存檔後當場就會產生那筆交易
- **範本可以在設定頁直接建立與修改**：設定 → 範本 → 新增
  - 以前只能在記帳時按「存為範本」，而且建好之後只能改名
  - 現在單擊範本列就能改內容（金額、分類、帳戶都可以改）
- **借貸對象可以填期初餘額**：新增對象時可填「他已經欠我 5000」或「我已經欠他 3000」
  - 不必把以前的帳一筆筆補記
  - 這筆**不會影響任何帳戶餘額**（錢是過去就流動過的），只計入借貸未結清金額與淨資產
  - 之後在借貸明細登錄還款時，才會實際影響帳戶

### 調整
- **可挑選的圖示從 1422 個增加到 1988 個**：新增全部品牌商標（LINE、Apple、Facebook 等）
  - 選圖器上方多了「實心／品牌」分頁，往下捲會自動載入更多，也可以點「載入更多」
  - 不收線條版：它那 169 個全是實心版的同名細線款，收進來只會讓每個名字出現兩次
- **分類與標籤的顏色從 15 色增加到 24 色**：補上棕、橄欖、松綠、深湖藍、天藍、亮紫等色系，排成整齊的 3 列
- **明細頁搜尋的分類選單不再收支混雜**：改成先列「支出」一段、再列「收入」一段
- **範本與週期性收支的清單支援左滑**：單擊列＝編輯，左滑出現「改名／刪除」（週期是「暫停／啟用」「刪除」）
- **日期欄位**：週期規則的首次發生日、借貸期初的起算日同樣整框可點開日曆

### 修正
- **每月 31 號的週期規則會永久跑掉**：原本 2 月被自動改成 28 號之後，3 月起就再也回不到 31 號，會一路停在 28 號
  - 現在改成記住你指定的「幾號」，遇到沒有那天的月份才落在該月最後一天，下個月自動回到原本的日子
  - 年度規則的 2/29 同樣修正（原本會跑到 3/1，現在落在 2/28，遇閏年回到 2/29）
  - 既有規則不受影響，行為與先前完全一致

## v1.5.0 — 2026-08-04

> 台股配息（現金股利＋股票股利）、餘額調整（對帳用）、全站日期欄位改善。**已測試驗收通過**。

### 新增
- **股利記錄**：明細 → 股票 → 新增，把「買進／賣出」切到「股利」
  - 填「每股現金股利」會依除權息日當天的持股自動算出總額；與券商通知不符可直接改
  - 現金股利在**發放日**入帳到指定銀行；**配股只增加股數、不增加成本**，均價會跟著下降
  - 匯費與二代健保補充保費分開填
  - 股利**不計入收支報表**（與股票買賣本金同樣是投資範疇），只影響資產與投資報表
  - 報表 → 投資新增「年度股利」卡（現金股利合計、配股股數、已實現＋股利）；持股預覽與投資總覽顯示累計配息
  - 同一次除權息又配息又配股時記成一筆即可
- **餘額調整**：對帳時把帳戶校正成實際金額
  - 入口：設定 → 帳戶 → 編輯（期初餘額下方），或**首頁帳戶列往左滑**
  - 填的是「那天實際上有多少錢」而不是差額——**設定後，之後補記或刪除基準日以前的記錄，這天起的餘額都不會再變**
  - 明細列顯示這次校正了多少；補記舊帳後這個差額會自動跟著修正
  - 基準日**當天**的記錄會一併被吸收，當天稍後才補的帳請記在隔天
  - 目前開放現金與銀行帳戶（信用卡的帳單期別是另一套計算，錨定會讓兩邊對不起來）

### 調整
- **日期欄位整個框都能點開日曆**：先前在電腦版只有右邊緣一小塊能觸發，很難點中；全站 15 處日期輸入一次修正（手機原本就正常，不受影響）

## v1.4.0 — 2026-08-03

> 明細頁互動翻轉：單擊＝預覽、左滑＝動作列，取代長按預覽。**真機驗收通過**。（補記）

### 調整
- **單擊明細列改為開預覽卡**：原本是直接進編輯頁，代價太高又容易誤存
- **左滑明細列露出動作抽屜**（Gmail 式）：編輯、刪除都收在裡面；卡片頁的「延後／收回」也移進抽屜
- 帳本、日曆、搜尋結果、卡片頁、股票持股與交易紀錄、發票列全部適用
- 發票列上只留「歸帳」，其餘動作進抽屜；品項明細改看預覽卡
- 移除長按預覽（由單擊取代）

## v1.3.0 — 2026-08-01

> 全站 UI 一致性審查後的 12 項修正＋長按預覽。**真機驗收通過**。（補記）

### 新增
- **長按預覽**（帳本／股票／發票）：長按可看唯讀卡片，不必進編輯頁（v1.4.0 起改為單擊）

### 修正
- **封存有餘額的帳戶前會先提醒並顯示餘額**：先前封存會讓淨資產無聲減少
- **首頁「較上月」改用上月底的淨資產快照**：先前拿今天的持股市值去比，股價漲跌被兩邊抵銷掉
- **遮蔽金額改為全站一致並記住設定**：明細列的金額先前完全不會被遮
- 明細頁月份導航擋掉未來月份（與報表頁同一口徑）
- 明細頁搜尋改為只篩選目前所在的子分頁；發票可搜店名、發票號碼、品項名
- 首頁點證券帳戶會帶入帳戶篩選
- 報表投資頁改為跨期分析視角（年度已實現損益、投資佔淨資產比重），持股清單與操作集中在明細 → 股票
- 徽章顏色語義統一（完成狀態一律綠色）、拆帳與分期可區分

## v1.2.0 — 2026-07-28

> 六項使用體驗修補（借還款分批結清、分類圖示配色、記帳動作列改版、帳戶跳轉、發票備註、信用卡延後入帳＋期別導航）＋兩個信用卡帳單 bug 修復。**已測試驗收通過**。

### 新增
- **借還款分批結清**：與對象「結清」時可勾選要結清的項目，不再是一次全部結清；未勾選的維持原未結清金額
- **首頁帳戶跳轉**：點現金／銀行帳戶直接進明細頁並篩選出該帳戶的記錄（信用卡、證券帳戶維持原本各自的跳轉）
- **信用卡延後入帳**：店家還沒向銀行請款、對帳金額對不上時，可把該筆消費的入帳日延到下一期帳單；卡片頁明細每列與標題列都有「延後」鈕，已延後的項目可隨時「收回」
- **信用卡期別導航**：卡片頁改成像明細頁一樣可左右翻頁切換帳單期別（可回看過去 12 期、預覽下一期），取代原本點不進去的「近 6 期一覽」

### 調整
- **記帳頁分類圖示跟著分類顏色**：先前只有明細列、分類管理會顯示自訂顏色，記帳表單一律是品牌藍
- **記帳頁動作列改版**：原本「代墊」按鈕＋「由他人代墊」開關兩種不同操作方式，統一成「拆帳／應收／應付／進階」四顆按鈕，應收應付互斥時會顯示為不可點
- **發票歸帳備註帶入金額**：先前只有「品名×數量」，現在會加上金額（如「茶葉蛋×2 $30、鮮奶 $75」），方便對帳

### 修正
- **刪除信用卡繳費記錄後帳單仍顯示「已繳」**：連帶會讓推播不再提醒該期繳費；現在刪除繳費記錄會一併清掉繳費狀態，帳單與提醒都會恢復正常

## v1.1.4 — 2026-07-26

> 專案標籤（Tag）。**已測試驗收通過**。

### 新增
- **專案標籤**：可為記錄打上像 `#福岡自由行` 這樣的標籤，一筆可多個；**拆帳時每一列可各自打不同標籤**
  - 設定 →「標籤管理」新增／改名／換色／刪除（搜尋列＋依名稱排序）
  - 記帳表單常駐「標籤」列，拆帳時改為每一列各自設定，新拆的列會自動帶入第一列的標籤
  - 明細列會顯示標籤 chip（最多 2 個＋餘數）
  - 明細搜尋可依標籤篩選（可多選，符合任一個即算），篩選後的合計只計算掛上該標籤的那部分金額（同一筆裡沒打標籤的拆帳不會被算進去）
  - 備份 CSV 的「標籤」欄自動帶出資料，不需另外處理

## v1.1.3 — 2026-07-26

> 分類圖示自由選、商家別名排序搜尋、AI 輔助發票分類。**已實測驗收通過**。

### 新增
- **分類圖示可自由挑選**：編輯分類時按「更多圖示…」可從完整的 Font Awesome 圖示庫（1400 多個）挑，支援中文搜尋（打「咖啡」找得到杯子類圖示）、英文名稱與舊版名稱
  - 圖示資料只在打開選圖器時才下載，不影響 app 啟動速度與離線快取大小
- **AI 輔助發票分類**：未歸帳的發票會自動建議分類，點歸帳時直接帶入（仍需按儲存才成立，不會自動記帳）
  - 兩層判斷：先比對你自己的歷史記錄（常去的店幾乎必中、不花錢），只有沒買過的新商家才問 AI
  - 發票列上會標示建議來源是「歷史」還是「AI」
  - 發票匣右上「分析」鈕可手動重跑；每日爬蟲同步完也會自動分類
- **商家別名搜尋**：別名清單加搜尋框，店名與冗長公司名都能搜

### 調整
- 商家別名清單改**依名稱排序**（中文依筆畫），不再是依比對字串長度
- **明細列改顯示子分類的圖示**：先前一律顯示母分類圖示，與分類選擇器不一致
- **分類選擇器的選中狀態改用該分類的自訂顏色**，不再一律是品牌藍（沒設顏色的分類外觀不變）
- 取消原訂的「發票 CSV 匯入」備援功能：爬蟲已穩定運作，此路徑無實際需求

## v1.1.2 — 2026-07-25

> 借貸功能補完（欠款查詢／還款登錄／淨額結清／反向代墊）＋三個 bug 修復。**已真機驗收通過**。

### 新增
- **借貸總覽**：首頁「借貸」卡（在帳戶列表下方，沒有未結清時自動隱藏），依對象列出應收／應付與淨額
  - 點對象進明細：逐筆展開看還款記錄、登錄還款、刪除還款、跳去編輯原交易
  - **一次結清**：同一對象既有應收又有應付時（互相代墊），對每筆未結清各記一筆全額還款到同一帳戶，帳戶淨變動就是實際匯款金額，兩邊同時結清——不需要也不該另記轉帳
- **登錄還款**：先前 `repayments` 欄位雖存在卻沒有任何寫入介面，結清狀態永遠停在「未結清」；現在可逐筆登錄與刪除，金額以未結清餘額為上限
- **借貸對象管理**：設定 →「借貸對象」可新增／改名／刪除／排序；記帳的對象選擇器也能就地改名。有借還款記錄的對象不可刪（不連帶刪掉真實債權債務）
- **由他人代墊**：記支出時打開開關並選對象，自動同時記下「這筆支出」與「欠對方的錢」，現金淨變動為 0、支出照常統計

### 修正
- **記帳頁從桌面捷徑／推播開啟時會卡住**：叉叉按了沒反應、儲存成功也不會跳出（資料其實有寫入）。原因是這種開法沒有「上一頁」可回，關閉動作靜默失效；現在回不去就直接回首頁。信用卡詳情頁有同樣問題，一併修正
- **編輯借還款會清空已登錄的還款記錄**：改為儲存時不覆寫還款欄位；若把交易型別改掉（會讓還款失去意義），儲存前會明確告知將刪除幾筆
- **收入拆帳可標記「代墊」但金額會消失**：代墊只對支出成立，收入的拆帳列不再顯示該按鈕，畫面合計與實際存檔金額一致

## v1.1.1 — 2026-07-25

> 功能擴充計畫（docs/09）批次 7：Web Push 推播。**已真機驗收通過**（手機安裝 standalone PWA、實際收到推播）。

### 新增
- **推播通知**（Cloud Functions v2 ＋ VAPID web-push，非 FCM）：
  - 每日 21:00：當天還沒記帳時提醒
  - 每日 09:00 彙總：信用卡繳費（到期前 7 天／前 1 天／逾期）、股票交割款不足、週期提醒待確認、明日自動扣款預告、發票爬蟲停擺告警
  - 載具同步到新發票時即時推播
  - 設定頁「推播通知」：本裝置總開關、六個情境開關、發送測試通知
- 推播的判定邏輯與首頁鈴鐺共用同一套純函式（部署時複製，不維護第二份口徑）

### 修正
- PWA Service Worker：`importScripts` 與 fetch handler 原先被打包進非同步 factory 才執行，改為內聯 workbox runtime，讓兩者回到 top-level 同步註冊（影響可安裝性判定與離線穩定度）

## v1.1.0 — 2026-07-21

> 功能擴充計畫（docs/09）批次 1–6 ＋後續調整。**核心功能待登入實機驗收**（空資料冒煙與 build/lint 已通過）。

### 新增
- 明細全域搜尋：跨月，關鍵字／類型／分類／帳戶／日期區間／金額範圍組合
- 交易範本：記帳表單「存為範本」、記帳頁快速套用 chips、設定頁管理
- 商家維度：交易商家欄位＋商家別名（載具冗長公司名對應店名）＋商家排行
- 年度報表：收支頁月／年視角切換（年摘要、12 月柱狀、全年分類、商家排行）＋年度消費熱力圖
- 帳本日曆檢視：月曆格顯示當日支出，點日看當日交易
- 淨資產趨勢圖：每日快照累積，報表新增「資產」分頁（折線＋區間切換）
- 分類管理：自訂大／小分類、圖示、顏色、上下排序；分類色套用於選擇器、明細列、報表
- 帳戶自訂排序（上下箭頭）
- 證券帳戶可填期初持股（追蹤前已持有部位，只計市值成本、不扣交割銀行現金）

### 調整
- 設定頁改二層（選單 → 子區塊），避免設定項越加越長
- 發票：已歸帳可展開品項明細＋歸帳把明細帶入交易備註；手動新增的發票可編輯／刪除
- 帳戶可刪除（連同其交易一併刪除）

## v1.0.2 — 2026-07-20

### 修正
- 瀏覽器分頁 favicon 改用新 app 圖示（金幣原子），與 PWA icon 統一，取代舊藍底 `$`

## v1.0.1 — 2026-07-20

### 修正
- 全新 app 圖示（原子軌道＋金幣，手繪 `branding/icon.png`），全套 PWA icon 重產；來源大圖移出 `public/` 避免進預快取

## v1.0.0 — 2026-07-20

首個正式版（1.0 前的開發歷程見 git tag `v0.7.0` 與 `docs/05-roadmap.md` 階段 0–7）。

### 新增
- 完整記帳核心：帳戶／收支／轉帳／應收應付、拆帳、代墊，記錄日與入帳日雙軌
- 信用卡：帳單切期、繳費、分期、額度；台股現股：買賣、持股損益、T+2 交割、GAS 每日收盤價
- 電子發票載具匣：本機爬蟲每日自動抓＋歸帳／略過／手動新增
- 收支報表（分類 Donut、近 6 個月趨勢）與投資報表
- 備份匯出：JSON 全量＋交易 CSV（拆帳逐列展開）
- 深淺主題三段切換（淺／深／跟隨系統）
- 通知中心：週期提醒＋信用卡繳費提醒＋交割餘額不足
- PWA：安裝、離線、長按捷徑（記一筆／發票匣）
- 版本顯示與 in-app 更新機制：開啟自動檢查，新版出橫幅、設定頁可手動檢查更新

### 修正
- PWA 無法安裝：補產 manifest 引用的 PNG 圖示（含 maskable 與 apple-touch-icon）
